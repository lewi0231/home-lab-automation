apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: production-tls
  namespace: production
spec:
  secretName: production-tls
  issuerRef:
    name: cluster-issuer-production
    kind: ClusterIssuer
  dnsNames:
    - work-rate-calculator.flowerhead.dev
