apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: personal-blog
  namespace: production
  annotations:
    kubernetes.io/ingress.class: traefik-public
spec:
  entryPoints:
    - web

  routes:
    - match: Host(`blog.flowerhead.dev`)
      kind: Rule
      services:
        - name: personal-blog
          port: 80
