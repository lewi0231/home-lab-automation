apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: development-tls
  namespace: development
spec:
  secretName: development-tls
  issuerRef:
    name: cluster-issuer-production
    kind: ClusterIssuer
  dnsNames:
    - dashboard.flowerhead.dev
