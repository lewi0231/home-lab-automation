apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: development
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system # Reuse the same secret as flux-system
  url: ssh://git@github.com/lewi0231/home-lab-automation
